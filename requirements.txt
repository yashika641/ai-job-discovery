requests>=2.31
tenacity>=8.2
pyyaml>=6.0
pydantic>=2.6
jinja2>=3.1
python-dotenv>=1.0
beautifulsoup4>=4.12
lxml>=5.1
feedparser>=6.0
openpyxl>=3.1
google-genai>=1.0   # Gemini API client, used by gemini_extractor.py for JD skill extraction

# dev / test
pytest>=8.0
responses>=0.25

# optional: only needed for scripts/detect_ats_browser.py (occasional manual
# re-check tool, not used by the daily pipeline). After installing, run:
#   python -m playwright install chromium
playwright>=1.40
